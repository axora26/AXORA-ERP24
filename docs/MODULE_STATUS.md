# AXORA-ERP24 — Statut des modules

Vocabulaire obligatoire : `NOT_STARTED`, `FOUNDATION`, `IN_PROGRESS`, `IMPLEMENTED_NOT_VERIFIED`, `VERIFIED`, `BLOCKED`, `NOT_TESTED`, `DEPRECATED`.

Aucun module n'est marque `VERIFIED` sans preuve CI reelle (numero de run + SHA exact). **La CI GitHub Actions est actuellement bloquee** : chaque job echoue en ~2 s sans journal (runs 3 a 7 sur `feat/foundation`), symptome d'un blocage de facturation/quota du compte GitHub — cause externe, hors du code. Tous les statuts ci-dessous s'appuient donc sur des executions **locales** reelles (PostgreSQL reel, API et navigateur reels) et restent au plus `IMPLEMENTED_NOT_VERIFIED`.

| ID | Module | Statut | Preuve locale | Reste a faire |
|---|---|---|---|---|
| INC-00 | Socle technique + Design System + Shell | `IMPLEMENTED_NOT_VERIFIED` | Monorepo pnpm, CI ecrite, shell web a routes par module, palette Ctrl K, kit UI partage, demarrage `pnpm local`, builds API/web de production OK. | Run CI distant ; theme sombre ; service worker PWA (INC-24). |
| INC-01 | Core — Identity / RBAC / Audit / MFA | `IMPLEMENTED_NOT_VERIFIED` | Sessions opaques, RBAC deny-by-default, limitation des connexions, `/auth/context`, garde de perimetre entreprise, numerotation atomique. Administration : utilisateurs, roles personnalises (matrice de permissions), entreprises, journal d'audit filtrable en lecture seule, garde du dernier OWNER, desactivation = revocation des sessions (16 tests e2e). Mot de passe en libre-service (revocation des autres sessions). MFA TOTP RFC 6238 : secret chiffre AES-256-GCM, anti-rejeu, defi a usage unique limite a 5 essais (6 tests e2e + 9 tests unitaires dont vecteurs RFC), parcours verifie dans Chromium. | Delegations temporaires, roles a portee projet, MFA imposee par politique d'organisation, "step-up" MFA sur actions critiques. |
| INC-02 | CRM — prospects / opportunites / pipeline | `IMPLEMENTED_NOT_VERIFIED` | 16 tests e2e CRM, parcours prospect -> opportunite verifie en navigateur. | Edition/suppression comptes et contacts, pagination, reorganisation du pipeline. |
| INC-03 | Etudes / DQE / BPU | `IMPLEMENTED_NOT_VERIFIED` | Tests e2e estimation (DQE finalise immuable, decimales exactes), interface Etudes & DQE. | Bibliotheque d'ouvrages, variantes, export Excel/PDF. |
| INC-04 | Devis -> Contrat | `IMPLEMENTED_NOT_VERIFIED` | 8 tests e2e (devis uniquement depuis DQE finalise, contrat uniquement depuis devis accepte, lignes figees). | Versions de devis, archivage de contrat. |
| INC-05 | Projets & Construction | `IMPLEMENTED_NOT_VERIFIED` | Projet issu d'un contrat ACTIF (un seul projet racine par contrat, montant et devise figes, WBS importable depuis les lignes), numerotation PRJ-AAAA-NNNN, WBS arborescent avec budget porte par les feuilles uniquement (parents derives, sans double comptage), baseline budgetaire figee, avenants avec separation des devoirs (demandeur != approbateur), budget revise = initial + avenants approuves, avancement physique pondere derive des taches, jalons (retard calcule), registre des risques (score P x I), transitions de statut controlees (demarrage apres baseline, cloture sans tache ouverte ni avenant en attente, motif obligatoire pour suspension/annulation), verrou pessimiste par projet. 12 tests e2e. Cockpit, WBS, Gantt, avenants, jalons/risques verifies dans Chromium. | Engage/consomme/facture/paye alimentes par INC-06/07/08/09 ; roles a portee projet ; situations de travaux ; export. |
| INC-06 | Achats | `IMPLEMENTED_NOT_VERIFIED` | Fournisseurs (numerotation FRN, evaluation qualite/delais/prix), demandes d'achat (DA) sur feuilles WBS dans la devise du projet, soumission par le demandeur et approbation par un tiers (separation des devoirs, rejet motive), consultation : offre par fournisseur chiffrant chaque ligne, comparatif trie, commande (BC) uniquement depuis une DA approuvee et une offre retenue (une seule par DA, offre expiree refusee), emission = engagement projet en temps reel (cockpit "Engage"), reception partielle idempotente sous verrou pessimiste (course entre deux receptions testee : une seule passe), "Consomme" projet = valeur recue, annulation uniquement avant reception (DA redevient commandable). 12 tests e2e. Ecrans demandes/comparatif/commandes/receptions verifies dans Chromium. | Rapprochement 3-way avec factures (INC-08), articles stockes (INC-07), seuils de validation par montant, retours fournisseurs. |
| INC-07 | Stock & Logistique | `IMPLEMENTED_NOT_VERIFIED` | Articles (seuil de reapprovisionnement, etiquette QR), depots et magasins de chantier rattaches a un projet, grand livre append-only **garanti en base** (trigger refusant UPDATE/DELETE, egalement pose sur le journal d'audit), soldes non negatifs **garantis en base** (CHECK), valorisation au cout moyen pondere sans residu d'arrondi, sorties chantier imputees au projet (cockpit "Consomme"), retours, transferts a valeur conservee, ajustements motives (permission distincte), inventaires physiques gelant le magasin et transformant les ecarts en ajustements traces, idempotence et verrou pessimiste (course entre deux sorties testee), reception d'achats d'articles stockes = entree en stock dans la devise de reference de l'entreprise (aucune conversion implicite). 12 tests e2e. | Reservations, numeros de serie/lots, inventaire mobile (scan) via PWA (INC-24), multi-devises de valorisation. |
| INC-08 | Finance (AR/AP) | `IMPLEMENTED_NOT_VERIFIED` | Taux de taxe parametres par entreprise (aucun taux presume), comptes bancaires/caisses avec solde calcule, factures clients (brouillon sans numero, numero legal FAC-AAAA-NNNN attribue a l'emission, chronologie garantie, facture de situation en % du contrat, cumul facture <= montant du contrat sous verrou), factures fournisseurs avec rapprochement 3-way commande/reception/facture (ecarts de quantite et de prix detectes et justifies), anti double facturation (reference fournisseur unique), validation par un tiers avant paiement, paiements partiels anti-doublon (verrou, idempotence, course testee, CHECK en base "jamais plus paye que du", paiements append-only par trigger), aucune conversion de change implicite, cockpit projet Facture/Paye (HT) et Facture client/Encaisse. Facture imprimable A4 et PDF reel genere par Chromium. 10 tests e2e. | Avoirs, relances, rapprochement bancaire, previsions de tresorerie, ecritures comptables et plan de comptes (a parametrer, aucune norme presumee). |
| INC-09 | Ressources Humaines | `IMPLEMENTED_NOT_VERIFIED` | Departements, employes (numerotation EMP, contrat, badge/QR/PIN unique, competences et habilitations avec echeance), donnees salariales (cout horaire, salaire) masquees sans `hr.payroll.read` et modifiables seulement avec `hr.payroll.manage`, chaine capture -> identification -> presence : borne de pointage par badge/QR/PIN, saisie manuelle tracee, sequence entree/sortie coherente sous verrou, pointages append-only **garantis en base** (trigger), feuilles de temps hebdomadaires (semaine normalisee au lundi, 24 h/jour max, imputation sur feuille WBS, heures pointees en regard), validation par une personne distincte de l'employe et de l'auteur de la soumission, cout fige a la validation (heures x cout horaire) et impute au "Consomme" projet, conges en jours ouvres calcules par le serveur (chevauchement refuse, decision par un tiers), preparation de paie mensuelle refusee tant qu'une feuille de la periode n'est pas validee, elements variables explicites, brut jamais negatif, cloture definitive, **aucune retenue legale presumee** (`statutoryDeductions: NOT_CONFIGURED`), aucune conversion de devise implicite. 9 tests e2e + 2 tests unitaires calendrier. Ecrans employes/presence/temps/conges/paie verifies dans Chromium. | Parametrage pays (cotisations, impots, net a payer), bulletins, soldes de conges acquis, biometrie (source prevue, capteur NOT_TESTED), pointage mobile hors ligne (INC-24). |
| INC-10 | Field/Chantier + GED | `IMPLEMENTED_NOT_VERIFIED` | **GED** : fichiers immuables adresses par SHA-256 (televersement idempotent, type detecte sur la signature binaire, HTML/SVG refuses, servis avec `nosniff`), arborescence par projet, documents numerotes DOC, revisions A/B/C a contenu immuable **garanti en base** (trigger), cycle brouillon -> visa -> approuve/refuse -> archive avec approbation par une personne distincte de l'auteur et du soumetteur, indice applicable conserve pendant un nouveau visa, recherche (code, titre, mots-cles, nom de fichier). **Chantier** : zones, journal quotidien (effectif declare vs pointages RH, sorties de stock du jour) signe puis fige (trigger), photos/observations horodatees a la prise de vue, geolocalisees en option, append-only et toujours rattachees a une reserve/tache/zone/journal (CHECK), reserves avec responsable et echeance, levee uniquement sur photo de correction posterieure au dernier refus et verifiee par un tiers (CHECK en base). **Hors ligne** : toute saisie passe par une file IndexedDB puis `POST /field/sync` (identifiant client idempotent, verrou optimiste, conflit renvoye avec l'etat serveur et resolu explicitement). 7 + 9 tests e2e, 5 tests unitaires de la file ; scenario reel verifie dans Chromium : saisie reseau coupe -> file persistee -> reprise -> synchronisation, et conflit entre deux appareils resolu explicitement. | Pilote de stockage S3 (NOT_TESTED), signatures electroniques (evenements de signature), RFI/transmittals/submittals, droits par projet, checklists terrain, service worker pour l'ouverture hors ligne (INC-24). |
| INC-11 | QHSE | `IMPLEMENTED_NOT_VERIFIED` | Checklists (points critiques), inspections renseignees uniquement par l'inspecteur designe, cloture exigeant tous les points, taux de conformite calcule, **une NCR ouverte automatiquement par point non conforme**, checklist figee apres cloture (trigger). NCR numerotees : faits constates **immuables et jamais supprimes en base** (trigger), actions correctives avec responsable et echeance (retard calcule), realisation avec preuve, refus motive, verification par une personne distincte du realisateur et cloture de la NCR par une personne distincte du createur (**CHECK en base**), toutes actions verifiees exigees. Incidents de securite a faits immuables et non supprimables (audit renforce avec les faits), jours d'arret uniquement prolongeables, cloture apres analyse et NCR liees soldees. Permis de travail (fenetre <= 14 j, delivrance par un tiers, actif/expire calcule). Quarts d'heure securite emarges par des employes RH (append-only). KPI sur donnees reelles : NCR ouvertes/critiques, actions en retard, jours sans accident avec arret, **taux de frequence sur les heures validees RH**, conformite moyenne. 11 tests e2e + 1 test unitaire. | Taux de gravite, audits planifies, plans de prevention, registres reglementaires par pays (non presumes), export des indicateurs (INC-23). |
| INC-12 | Commissioning | `IMPLEMENTED_NOT_VERIFIED` | Mise en service par equipement MEP installe (une activite par equipement), fiches d'essai append-only a resultat **calcule par le serveur** sur les bornes d'acceptation, un echec documente obligatoirement ses anomalies (rattachees a l'equipement et a l'essai), correction declaree puis **retest reussi obligatoire**, reception par une personne distincte du dernier essayeur (l'equipement passe `COMMISSIONED`), remise au client avec DOE compose de documents GED **approuves**. **Sequence garantie en base** (trigger : precommissioning, essai reussi, anomalies soldees, retest posterieur a la derniere correction, receptionnaire distinct, DOE ; aucun retour arriere ni suppression). 7 tests e2e dont le DoD « reception bloquee sans retest » en API et en base. | Procedures types par famille d'equipement, essais multi-sous-systemes, signature client de la remise. |
| INC-13 | MEP | `IMPLEMENTED_NOT_VERIFIED` | Systemes par discipline (CVC, CFO, CFA, plomberie, incendie), equipements de reference (repere unique, caracteristiques, fiche technique GED) repris par le commissioning, quantitatif. **Noyau de calcul versionne** (8 relations physiques elementaires : debits air/eau, courants mono/tri, chutes de tension resistives, vitesse et diametre en section circulaire) en decimal 40 chiffres : chaque note expose entrees + unites, formule, formule substituee, hypotheses ; resultat toujours produit par le serveur ; **zones non couvertes declarees `NOT_STARTED`** (aucune norme codee sans source). Revisions immuables (trigger), validation par un ingenieur distinct de l'auteur (CHECK) avec sources exigees, revision validee applicable conservee. 7 tests unitaires sur cas de reference calcules independamment + 6 tests e2e. | Calculs normatifs (bilans thermiques, protections, pertes de charge, sprinklers) : non implementes faute de source verifiee ; coordination/synthese (INC-14 BIM). |
| INC-14 | BIM / IFC / Revit | `IMPLEMENTED_NOT_VERIFIED` (import IFC) · connecteur Revit natif `NOT_TESTED` | Lecteur STEP/IFC sans dependance (en-tete, structure spatiale, elements, niveaux, types, systemes, classifications, proprietes et quantites ; chaines encodees \\X2\\), **teste sur fichiers reels buildingSMART IFC2X3, IFC4 et IFC4X3_ADD2** (CC BY 4.0, empreintes documentees). Maquettes par projet, versions importees depuis un fichier dont **l'empreinte SHA-256 est recalculee a l'import** (et comparee a celle calculee dans le navigateur), reverifiable a la demande ; contenu de version immuable et elements append-only (triggers) ; visa par une personne distincte de l'importateur (CHECK) ; comparaison de versions par GlobalId ; liaison equipement MEP <-> element sans duplication d'identite ; conflits de synthese entre deux elements reels, discussion append-only, resolution approuvee par un tiers (CHECK). **Connecteur Revit : six etats exposes separement, tous `NOT_AVAILABLE`/`NOT_TESTED`** (aucun add-in livre, aucun Revit exerce) — jamais simule. 6 tests unitaires + 9 tests e2e ; import navigateur verifie dans Chromium. | Add-in Revit natif (necessite Windows + licence Revit : BLOCKED/NOT_TESTED), visionneuse 3D, detection geometrique automatique des conflits, quantites calculees depuis la geometrie. |
| INC-15 | Assets / GMAO | `IMPLEMENTED_NOT_VERIFIED` | **Passeport d'actif a origine tracee** : cree depuis une mise en service receptionnee (equipement MEP, activite CX, DOE remis) ou repris d'un existant avec justification obligatoire (CHECK `assets_origin_traceable`) ; un seul passeport par equipement ; jamais supprime (trigger). Preventif : plans a periodicite, **generation idempotente** des OT echus (unicite plan + echeance, verrou). Tickets -> **conversion explicite** en OT correctif (heure de panne reprise) ou rejet motive. OT : demarrage, temps au **cout horaire RH fige**, pieces sorties du **grand livre de stock** (mouvement `MAINTENANCE_ISSUE`, cout moyen pondere), cloture exigeant compte-rendu + temps saisi + remise en service posterieure a la panne pour un correctif (API **et** trigger) ; OT clos fige, temps/pieces append-only. **MTTR, MTBF, disponibilite calcules sur l'historique reel** des OT correctifs (union des indisponibilites, `null` sans panne — jamais estime), formules exposees ; taux de respect des delais (remise en service pour un correctif). 4 tests unitaires (noyau de fiabilite) + 9 tests e2e ; parcours OT verifie dans Chromium. | Planification des ressources/charge, contrats de maintenance et SLA, releves compteurs d'usage (declenchement a l'usage), application mobile technicien hors ligne (le module Chantier offre deja la synchro), couts sous-traitance. |
| INC-16 | Smart Building | `IMPLEMENTED_NOT_VERIFIED` (plateforme d'ingestion) · pilotes BACnet/Modbus/KNX/MQTT natifs `NOT_TESTED` (non developpes) | Batiments, passerelles a **jeton machine** (empreinte seule stockee, rotation, desactivation), points (reference protocolaire, plage plausible, ecriture avec tolerance, lien actif GMAO). **API d'ingestion unique** : lots valides lecture par lecture, **rejeu idempotent** (doublon), **conflit refuse et rapporte**, lectures append-only (trigger), hors plage conservees `BAD`, lectures tardives sans effet sur l'etat courant. **Alarmes** figeant regle, seuil et lecture declencheuse (trigger), acquittement trace, levee uniquement par lecture revenue a la normale. **Consignes** : demandee -> transmise -> acquittee par la passerelle -> **confirmee uniquement par relecture** (CHECK). **Cinq niveaux de connectivite distincts** (protocole / connecteur / passerelle joignable / lecture reelle / ecriture reelle) ; les deux derniers uniquement par attestation d'essai point-a-point, **refusee sur un simulateur** (API et trigger). Tendances brutes et horaires (min/moy/max). 4 tests unitaires + 7 tests e2e ; parcours verifies dans Chromium (consigne, acquittement, jeton). Demo : passerelle etiquetee SIMULATEUR alimentant l'API reelle + passerelle BACnet declaree jamais connectee (tout `NOT_TESTED`). | Pilotes natifs BACnet/IP, Modbus TCP, KNXnet/IP, client MQTT (aucun equipement physique disponible : `NOT_TESTED`), ordonnancement horaire, supervision graphique (synoptiques), stockage longue duree compresse. |
| INC-17 | Energie | `IMPLEMENTED_NOT_VERIFIED` | Compteurs identifies (batiment, pas 5 a 60 min, passerelle + reference ou import, actif GMAO) : reseau soutirage/injection, PV, groupe, batterie charge/decharge, consommation, gasoil. **Ingestion d'intervalles idempotente** par la passerelle (jeton) ou par import utilisateur (permission distincte) : meme debut + meme valeur = doublon, valeur differente = **conflit refuse et rapporte**, debut non aligne ou intervalle non termine rejete, intervalles append-only (trigger, valeurs >= 0). **Tarifs historises** append-only : cout calcule au tarif en vigueur chaque jour, « incomplet » si un tarif manque (jamais suppose). **Bilans avec couverture** par nature et par compteur, consommation mesuree ou bilan des sources (methode affichee), comparaison a la periode precedente **seulement si les deux couvertures >= 95 %**, part renouvelable (PV - injection), intensite kWh/m2 si surface renseignee. **Autonomie** batterie/cuve calculee uniquement sur niveau < 60 min et couverture >= 90 % des releves sur 24 h, avec entrees, formule et hypotheses exposees ; sinon « non calculable » avec la raison ; entrees simulees signalees. Alertes figees (pic d'intervalle ; depassement journalier uniquement sur jour complet), acquittement trace. 4 tests unitaires (noyau) + 2 web (import CSV) + 6 e2e ; import et bilan verifies dans Chromium. | Import de fichiers (factures PDF fournisseur), correction d'intervalle tracee, jours locaux (fuseau entreprise) au lieu de UTC, previsions, pilotage de charges, facturation interne par sous-compteur. |
| INC-18 | Gestion de parc | `IMPLEMENTED_NOT_VERIFIED` | Vehicules routiers, engins et machines **adosses a un passeport GMAO** cree dans la meme transaction (origine justifiee) : pannes et entretiens passent par les tickets et OT de la GMAO (invariant BC-18 n°3). **Compteurs km/heures append-only et monotones** (API + trigger : un releve ne recule jamais, y compris par rapport a un releve posterieur). **Affectation** : chauffeur RH actif **et habilite** (competence RH exigee et non expiree), vehicule actif **en regle** (assurance / carte grise / controle ou VGP selon la nature ; piece echue ou absente = refus explicite), releve de depart coherent ; **une seule affectation ouverte par vehicule et par chauffeur** (index uniques partiels). **Carburant** append-only (montant = litres x prix, CHECK), imputation au projet de l'affectation en cours, **consommation plein a plein** (L/100 km ou L/h, non calculee sans deux pleins complets). **Echeances reglementaires** historisees (valide / < 30 j / echu / absent). **Incidents** : conducteur deduit de l'affectation ouverte a l'instant des faits (jamais saisi), ticket GMAO a l'heure de la panne, immobilisation repercutee sur le passeport ; faits figes. **Cout de possession 12 mois** (carburant + OT GMAO + pieces + incidents) et cout au km/heure sur usage mesure. 3 tests unitaires + 6 e2e ; refus d'affectation verifie dans Chromium. | Carte carburant / import de transactions, telematique GPS (NOT_TESTED sans boitier), planification des affectations futures, amortissement comptable, refacturation interne aux projets (le carburant impute n'est pas ajoute au consomme projet pour eviter un double compte avec la facture du petrolier). |
| INC-19 | Sous-traitants | `IMPLEMENTED_NOT_VERIFIED` | **Reutilisation stricte** : le sous-traitant est un fournisseur des Achats (fiche de qualification rattachee), le lot confie reference une **commande emise** du sous-traitant et un **lot WBS** du meme projet, les factures de situation et de liberation sont des **factures fournisseurs de la Finance** (approbation et paiement par leurs flux). **Obligation de vigilance** : RCCM, attestations fiscale et sociale, assurance RC en vigueur exigees pour qualifier, confier un lot et approuver une situation ; qualification par une autre personne que le createur (CHECK). **Situation** : avancement cumule **derive des taches terminees du lot WBS** (poids), fige a la preparation, jamais saisi ; montant = lot x (cumul - certifie) sans derive d'arrondi ; retenue et net figes (trigger) ; approbation a quatre yeux (CHECK). **Retenue de garantie** objet de premier ordre : echeance explicite, liberation a echeance ou anticipee **contre caution bancaire referencee** (CHECK), facture de liberation separee. Commande support **ni receptionnable ni annulable** (pas de double compte) ; **consomme projet** enrichi des situations certifiees. 6 tests e2e. | Equipes et pointage des effectifs sous-traitants, penalites de retard, compte prorata, paiement direct du maitre d'ouvrage, notation qualite/HSE (reprise des NC QHSE par sous-traitant), revision de prix. |
| INC-20 | Portails Client & Fournisseur | `IMPLEMENTED_NOT_VERIFIED` | **Plan d'identite externe separe** (principal, invitation, session et cookie dedies ; aucune interoperabilite avec les sessions internes, prouvee dans les deux sens). Principal rattache a **une societe et un enregistrement racine** (compte CRM ou fournisseur), immuable (CHECK + trigger). **Invitation a usage unique** (empreinte seule, jeton dans le fragment d'URL, 7 j), activation par la personne, mot de passe >= 12 caracteres, connexion limitee, pas d'enumeration par chronometrage. **Deny-by-default** : exposition explicite par ressource, uniquement si elle appartient a l'enregistrement racine et est publiable, **re-verifiee a chaque lecture** ; client : projets (avancement, jalons — jamais de couts), factures emises, documents approuves (telechargement audite) ; fournisseur : commandes (accuse de reception avec date confirmee, append-only) et ses factures. **Suspension/revocation** : sessions et invitations invalidees dans la meme transaction (trigger), revocation definitive. Actions externes auditees (acteur null + principal). 6 tests e2e ; parcours client et fournisseur verifies dans Chromium. | MFA externe, depot de factures fournisseur par le portail, messagerie/visa client, notifications e-mail (aucun serveur SMTP configure : NOT_TESTED), SSO. |
| INC-21 | Workflow Engine + Automatisation | `IMPLEMENTED_NOT_VERIFIED` | **Evenement -> condition -> action**. Sept evenements types emis par les modules **dans la transaction metier** (outbox : demande d'achat soumise, facture fournisseur enregistree, NC ouverte, ticket GMAO, alarme GTB, incident de parc, situation de sous-traitance). Definitions **versionnees** (une version est immuable, une seule active par code, jamais retroactive), conditions typees (decimal exact / texte), actions NOTIFY (role), REQUIRE_APPROVAL (role approbateur, SLA, escalade unique vers un role d'escalade), WEBHOOK. **Execution idempotente** (unique definition x evenement, effets dans la meme transaction) et **journal append-only** des succes et des echecs (destinataire absent, approbateur indisponible -> approbation maintenue, fail-closed). **Approbations a quatre yeux** (CHECK en base, role approbateur requis) ; **barriere** sur l'approbation des demandes d'achat, factures fournisseurs et situations : refus tant qu'une approbation est en attente/refusee ou qu'un evenement n'est pas encore evalue. **Webhooks** signes HMAC-SHA256 (`v1=` sur `horodatage.corps`), re-signes a chaque tentative, secret montre une fois et chiffre au repos (AES-256-GCM), politique anti-SSRF (HTTPS, pas d'adresse privee, pas de redirection), 5 tentatives (1 min -> 2 h), relance manuelle. Notifications personnelles (cloche). 10 tests e2e (recepteur HTTP reel, signature verifiee) + 5 unitaires moteur + 3 web ; barriere et cloche verifiees dans Chromium. | Editeur visuel de flux (graphe), conditions OU/imbriquees, actions de modification de donnees (volontairement exclues), notifications e-mail/SMS (aucun fournisseur configure : NOT_TESTED), resolution DNS anti-rebinding des cibles webhook. |
| INC-22 | AI / Copilot | `IMPLEMENTED_NOT_VERIFIED` | **Copilote ancre et deterministe** (aucun modele generatif appele : `modelProvider = null`, moteur `axora-grounded-v1`). **Herite strictement des droits** : les permissions consultees sont celles resolues par la garde RBAC pour la requete ; `ai.copilot.use` n'ouvre aucune donnee, chaque outil exige la permission de lecture de son module (12 outils : creances, factures fournisseurs, demandes d'achat, commandes, projets, QHSE, GMAO, alarmes GTB, parc, RH, pipeline, approbations), la synthese reutilise les sections du tableau de bord deja soumises a leur permission, la consultation par reference (PRJ, DA, BC, FF, FAC, NC, OT, WFA) reste dans le perimetre entreprise et repond comme pour une piece inexistante hors perimetre. **Chaque affirmation est citee** ([n] -> ressource reelle avec lien). **Preuve d'inference append-only** pour chaque question (y compris refus et hors perimetre) : question, mode, controles d'acces accordes ET refuses, sources, reponse, **empreinte SHA-256 verifiee par la base (CHECK)**, preuve liee au proprietaire de la session (trigger). Question hors perimetre : aucune reponse inventee, liste de ce que l'utilisateur peut demander. Aucun chemin d'ecriture (liens vers les ecrans existants). Conversations privees a leur auteur ; journal des preuves auditable avec `ai.evidence.read`. 9 tests e2e (preuve RBAC : toute source citee provient d'un outil accorde) + 5 unitaires ; refus RBAC constate dans Chromium. | Generation en langage naturel par un LLM (aucun fournisseur ni cle configure : NOT_TESTED — le branchement devra reformuler uniquement les faits sources), comprehension semantique au-dela des mots-cles, recherche documentaire (GED) plein texte, actions proposees executables. |
| INC-23 | Analytics/BI + API publique | `IMPLEMENTED_NOT_VERIFIED` (API & analytique) · connecteurs externes `NOT_TESTED` (registre) | **Analytique livree** : 11 indicateurs mensuels (facturation, encaissements, decaissements, commandes, affaires gagnees, NC ouvertes/cloturees, incidents, OT dans les delais/en retard, heures validees, energie consommee/importee/produite hors donnees simulees, carburant) calcules a la demande sur les donnees reelles du perimetre entreprise, en decimal exact, **une serie par devise (jamais additionnees)**, nombre d'enregistrements sources et fenetre explicites. **RBAC par indicateur** : `analytics.report.read` n'ouvre que les modules deja lisibles. Export CSV (memes droits). **Instantanes mensuels figes** (append-only, versionnes, fenetre calendaire verifiee par CHECK, marquage DEMO de l'organisation, indicateurs non figes faute de droits listes, relus selon les droits du lecteur). **Tableaux de bord configurables** (indicateurs limites aux droits du createur, partage en lecture, proprietaire seul editeur, perimetre immuable en base). 6 tests e2e + 5 unitaires + 3 web ; parcours verifie dans Chromium (graphiques, infobulle, CSV, tableau partage filtre pour la DAF). **API publique v1** : cles d'API a **permissions explicites** issues d'un catalogue public (jamais d'acces total), **jamais plus que les droits de l'emetteur**, et **re-intersectees a chaque requete** avec les droits actuels du createur (retrait immediat ; createur desactive = cle suspendue) ; secret montre une fois, empreinte SHA-256 seule en base ; **limitation par minute et quota journalier** (compteurs atomiques, en-tetes Retry-After / X-RateLimit), **liste d'IP**, expiration, **revocation definitive** et permissions figees (triggers) ; limiteur d'essais invalides par IP ; journal append-only des requetes authentifiees (refus 403/429 compris) ; routes paginees par curseur (projets, factures clients et fournisseurs, commandes, OT, series analytiques) et creation de prospect CRM par la meme regle que l'interface ; **OpenAPI 3.1** servie et verifiee par test (chaque route documentee avec sa permission). **Webhook entrant** (formulaire -> prospect CRM) : signature HMAC verifiee sur le **corps brut** avant tout traitement, anti-rejeu 5 min, schema strict, **idempotence par identifiant d'evenement** (unique en base, prospect et trace dans la meme transaction), refus traces, limiteur de signatures invalides **par point d'entree**. **Registre des connecteurs** a grille de verite (supporte / implemente / externe atteint / lecture / ecriture) : aucun connecteur non raccorde a un systeme externe reel n'est marque verifie, et le mot « TESTED » n'est jamais employe (invariant teste). 6 + 9 tests e2e, 5 + 4 + 3 unitaires ; demo : appels reels 200/200/403, webhook 201 puis 200 idempotent ; ecrans verifies dans Chromium. | Webhooks entrants d'autres types, rotation de cle assistee, quotas par IP sans cle, portail developpeur public, SDK clients ; connecteurs externes (SMTP, SMS, S3, BACnet/Modbus/KNX/MQTT, banque, LLM) non developpes ou `NOT_TESTED`. |
| INC-24 | Multi-plateforme (PWA/Windows/Android/iOS) | `NOT_STARTED` | — | Manifest PWA present, service worker absent. |

## Gates locaux (derniere execution)

2026-09-25 — `pnpm typecheck` OK · `pnpm lint` OK · `pnpm test` 130 tests PASS · `pnpm test:e2e` 246 tests PASS (PostgreSQL 16 local) · `nest build` + `next build` OK · ecrans verifies dans Chromium sans erreur console · facture PDF generee · scenario hors ligne et conflit terrain verifies dans Chromium.
